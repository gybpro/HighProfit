const regExp = {
    phone: /^1[3-9]\d{9}$/,
    // 密码正则，最少6位，包括至少1个大写字母，1个小写字母，1个数字，1个特殊字符
    // pwd: /^(?=.*\d)(?=.*[A-Z])(?=.*[a-z])(?=.*[!@#$%^&*?]).{6,}$/,
    // 同时包含数字和字母，6到20位，.匹配除\n和\r之外的单个字符，*匹配任意次，/i忽略大小写
    pwd: /^(?=.*\d)(?=.*[a-z]).{6,20}$/i,
    idCard: /^\d{11}[0-9x]{4}|\d{14}[0-9x]{4}$/,
    replace: {
        money: /\B(?=(\d{3})+\b)/g,// 千分符
    }
}

export default regExp;
